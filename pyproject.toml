[project]
name = "supervaize-hello-world"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.128.0", "rich>=14.3.1", "supervaizer>=0.10.12"]

# FOR DEVELOPMENT ONLY - DO NOT COMMIT THIS
[tool.uv.sources]
supervaizer = { path = "../supervaizer", editable = true }
